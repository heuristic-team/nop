fn add(a: i64, b: i64) i64 = a + b

fn foo(a: i64, mut b: i64) i64 = {
    b = {
        d := a - 4
        b * d
    }
    for true do {
      mut g := 34
    }
    mut c: i64 = add(a, b)
    mut f := add(c, b)
    c
}
